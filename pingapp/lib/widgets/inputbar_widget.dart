import 'package:flutter/material.dart';

class InputBarDesktop extends StatelessWidget {
  const InputBarDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}